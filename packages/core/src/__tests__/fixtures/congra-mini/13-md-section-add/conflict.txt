## Installation

<<<<<<< ours
Install via npm:

```bash
npm install gitwand
```

For local development, clone the repo and run `pnpm install`.
||||||| base
Install via npm:

```bash
npm install gitwand
```
=======
Install via npm:

```bash
npm install @gitwand/core
```

The package now ships ESM-only — make sure your project uses `"type": "module"`.
>>>>>>> theirs
