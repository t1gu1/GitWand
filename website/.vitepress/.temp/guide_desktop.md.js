import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Desktop App","description":"","frontmatter":{},"headers":[],"relativePath":"guide/desktop.md","filePath":"guide/desktop.md"}');
const _sfc_main = { name: "guide/desktop.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="desktop-app" tabindex="-1">Desktop App <a class="header-anchor" href="#desktop-app" aria-label="Permalink to &quot;Desktop App&quot;">​</a></h1><p>The GitWand desktop app is a lightweight, native Git client built with Tauri 2 and Vue 3. It covers the full daily workflow and adds smart conflict resolution on top.</p><h2 id="repository-view" tabindex="-1">Repository View <a class="header-anchor" href="#repository-view" aria-label="Permalink to &quot;Repository View&quot;">​</a></h2><p>The main interface shows staged, unstaged, untracked, and conflicted files in a sidebar. The main area displays inline diffs. You can stage, unstage, and discard changes without leaving the interface.</p><p><strong>Partial staging</strong> is supported at the line or hunk level — select exactly what goes into your next commit.</p><h2 id="commit-workflow" tabindex="-1">Commit Workflow <a class="header-anchor" href="#commit-workflow" aria-label="Permalink to &quot;Commit Workflow&quot;">​</a></h2><ul><li>Summary and description fields</li><li>Optional commit signature</li><li><code>Ctrl+Enter</code> / <code>Cmd+Enter</code> shortcut to commit</li><li><strong>Amend unpushed commits</strong> — a pencil icon appears on hover in the history log, opening an overlay pre-filled with the existing message</li></ul><h2 id="branches" tabindex="-1">Branches <a class="header-anchor" href="#branches" aria-label="Permalink to &quot;Branches&quot;">​</a></h2><p>Click the branch name in the header to:</p><ul><li>Search branches</li><li>Switch to a branch</li><li>Create a new branch</li><li>Delete a branch</li><li>Merge any branch via the merge button</li></ul><h2 id="merge-preview" tabindex="-1">Merge Preview <a class="header-anchor" href="#merge-preview" aria-label="Permalink to &quot;Merge Preview&quot;">​</a></h2><p>Before merging, GitWand predicts the outcome <strong>without touching the working tree</strong> using <code>git merge-base</code>, <code>git show</code>, and <code>git merge-file -p --diff3</code>.</p><p>The preview shows a per-file breakdown:</p><table tabindex="0"><thead><tr><th>Status</th><th>Meaning</th></tr></thead><tbody><tr><td>Auto-resolvable</td><td>GitWand can handle it automatically</td></tr><tr><td>Partial</td><td>Some hunks need manual resolution</td></tr><tr><td>Manual</td><td>Complex conflicts requiring human judgment</td></tr><tr><td>Add/delete</td><td>File added on one side, deleted on the other</td></tr></tbody></table><p>A badge summarises the overall result: <code>Clean merge</code>, <code>100% auto-resolvable</code>, or <code>N conflicts to review</code>.</p><h2 id="push-pull" tabindex="-1">Push &amp; Pull <a class="header-anchor" href="#push-pull" aria-label="Permalink to &quot;Push &amp; Pull&quot;">​</a></h2><p>One-click push and pull with badge counters showing ahead/behind commits. Auto-fetch runs in the background every 30 seconds.</p><h2 id="history-graph" tabindex="-1">History &amp; Graph <a class="header-anchor" href="#history-graph" aria-label="Permalink to &quot;History &amp; Graph&quot;">​</a></h2><p>Browse the full commit log in the sidebar. Click any commit to see its diff with a file list and scroll-spy highlighting.</p><p>Long commit descriptions collapse to 2 lines with an expand toggle.</p><p>A separate <strong>DAG graph view</strong> renders the full branch topology as an SVG with lane layout and ref badges.</p><h2 id="diff-viewer" tabindex="-1">Diff Viewer <a class="header-anchor" href="#diff-viewer" aria-label="Permalink to &quot;Diff Viewer&quot;">​</a></h2><ul><li>Side-by-side or inline toggle (persisted across sessions)</li><li>Syntax highlighting for 30+ languages</li><li>Word-level diff using LCS</li><li>Collapsible unchanged regions</li><li>Canvas minimap</li><li>Hunk navigation (prev/next)</li><li>Double-column line numbers</li></ul><h2 id="image-diff-viewer-v1-6" tabindex="-1">Image Diff Viewer (v1.6) <a class="header-anchor" href="#image-diff-viewer-v1-6" aria-label="Permalink to &quot;Image Diff Viewer (v1.6)&quot;">​</a></h2><p>Binary image files in a commit diff render side-by-side instead of the opaque &quot;Binary file changed&quot; line. Four comparison modes are available via a toggle:</p><table tabindex="0"><thead><tr><th>Mode</th><th>What it does</th></tr></thead><tbody><tr><td>Side-by-side</td><td>Before / after with dimension + size metadata overlay</td></tr><tr><td>Overlay</td><td>Stacked images with an opacity slider</td></tr><tr><td>Blink</td><td>Alternates between the two images to reveal pixel-level deltas</td></tr><tr><td>Slider</td><td>Draggable reveal between before and after</td></tr></tbody></table><p>Supported formats: <strong>PNG, JPG, WebP, GIF, SVG</strong>. GIFs get an &quot;animated&quot; badge. SVGs render as raster (so you see the actual image) — the text diff remains available via the file history. Images larger than 20 MB per side are skipped with an explicit notice to keep the viewer snappy.</p><p>Under the hood, a new <code>read_file_at_revision(path, rev)</code> backend command (available in both the Rust/Tauri and Node dev-server paths) fetches file bytes at any git revision, so the viewer can compare <code>HEAD^</code> vs <code>HEAD</code>, across branches, stashes, or arbitrary refs.</p><h2 id="folder-tree-in-commit-diff-v1-6" tabindex="-1">Folder Tree in Commit Diff (v1.6) <a class="header-anchor" href="#folder-tree-in-commit-diff-v1-6" aria-label="Permalink to &quot;Folder Tree in Commit Diff (v1.6)&quot;">​</a></h2><p>The file list on the right of the commit diff has a <strong>flat ↔ tree</strong> toggle. Tree mode collapses deeply-nested changes into an expandable folder tree with per-folder aggregates (files changed, additions, deletions), making a 200-file commit navigable at a glance.</p><ul><li>Click any folder to <strong>filter the list</strong> down to its descendants</li><li>Breadcrumb at the top surfaces the active filter with a clear (×) button</li><li>Sidebar is <strong>resizable</strong> — drag its left edge, or double-click to reset. Width is clamped 180–600 px and persisted across sessions</li><li>Keyboard navigation throughout (arrow keys, Enter, Esc)</li><li>Available in all five UI locales</li></ul><p>The backing <code>folder_diff</code> backend command (Rust/Tauri + Node dev-server + TypeScript wrapper) builds the aggregated tree.</p><h2 id="file-history-blame" tabindex="-1">File History &amp; Blame <a class="header-anchor" href="#file-history-blame" aria-label="Permalink to &quot;File History &amp; Blame&quot;">​</a></h2><ul><li>Full file history with <code>git log --follow</code></li><li>Blame view grouped by commit</li><li>Time-travel diff between any two versions of a file</li></ul><h2 id="repo-switcher" tabindex="-1">Repo Switcher <a class="header-anchor" href="#repo-switcher" aria-label="Permalink to &quot;Repo Switcher&quot;">​</a></h2><p>Click the repo name in the header to open a dropdown with all recently opened repositories. Pin favorites, remove entries, and switch instantly.</p><h2 id="pull-requests" tabindex="-1">Pull Requests <a class="header-anchor" href="#pull-requests" aria-label="Permalink to &quot;Pull Requests&quot;">​</a></h2><p>Browse, create, checkout, and merge GitHub PRs without leaving the app:</p><ul><li>PR list in the sidebar</li><li>Full detail view: diff, CI checks, comments, inline review</li><li>Inline comments anchored to diff lines with full threading</li><li>Code suggestions via <code>\`\`\`suggestion \`\`\`</code> blocks, applicable in one click</li></ul><h2 id="ai-everywhere-v1-3" tabindex="-1">AI Everywhere (v1.3) <a class="header-anchor" href="#ai-everywhere-v1-3" aria-label="Permalink to &quot;AI Everywhere (v1.3)&quot;">​</a></h2><p>AI is woven into every step of the workflow. Each suggestion is opt-in (explicit button or shortcut) and traceable (the prompt and provider used are visible). Works with any configured AI provider — Claude Code CLI, Claude API, OpenAI-compatible, or Ollama.</p><h3 id="pr-branch-workflow" tabindex="-1">PR &amp; branch workflow <a class="header-anchor" href="#pr-branch-workflow" aria-label="Permalink to &quot;PR &amp; branch workflow&quot;">​</a></h3><ul><li><strong>AI branch name</strong> — propose a <code>feat/…</code> / <code>fix/…</code> name from a description or the staged diff instead of typing one</li><li><strong>AI PR title &amp; description</strong> — <code>PrCreateView</code> analyses the commits in <code>currentBranch..base</code> and drafts a structured title + body (summary, test plan, breaking changes)</li><li><strong>Hunk-level critique in the PR Intelligence panel</strong> — per-hunk feedback on risks, regressions, and concrete suggestions (replaces the static-heuristics panel)</li></ul><h3 id="merge-rebase-insight" tabindex="-1">Merge &amp; rebase insight <a class="header-anchor" href="#merge-rebase-insight" aria-label="Permalink to &quot;Merge &amp; rebase insight&quot;">​</a></h3><ul><li><strong>Natural-language conflict explanation</strong> — in the merge editor, the <code>DecisionTrace</code> is rendered in plain English (&quot;this hunk changes the <code>login()</code> signature on both sides — manual required&quot;)</li><li><strong>Pre-merge AI risk summary</strong> — the merge preview complements the <code>merge-tree</code> simulation with an AI risk read (&quot;3 files touch auth, high regression probability if untested&quot;)</li><li><strong>Semantic squash in interactive rebase</strong> — AI groups candidate commits by intent and proposes a combined message</li></ul><h3 id="commit-stash" tabindex="-1">Commit &amp; stash <a class="header-anchor" href="#commit-stash" aria-label="Permalink to &quot;Commit &amp; stash&quot;">​</a></h3><ul><li><strong>AI stash message</strong> — before <code>git stash</code> (and in the switch-branch flow), AI proposes a message from the unstaged diff</li><li><strong>AI-ranked Absorb target</strong> — when the selected lines span multiple commits, AI ranks candidates semantically instead of taking the first <code>git blame</code> hit</li></ul><h3 id="history-search" tabindex="-1">History &amp; search <a class="header-anchor" href="#history-search" aria-label="Permalink to &quot;History &amp; search&quot;">​</a></h3><ul><li><strong>Natural-language commit search</strong> — query the log with plain-English questions (&quot;when did we introduce log pagination?&quot;)</li><li><strong>Blame context</strong> — &quot;why did this line change?&quot; button on each blame block, answered with context from neighbouring commits</li><li><strong>AI release notes generator</strong> — produce structured markdown (Added / Changed / Fixed) from <code>git log &lt;tag&gt;..&lt;tag&gt;</code>, ready to paste into a GitHub release</li></ul><h3 id="dashboard" tabindex="-1">Dashboard <a class="header-anchor" href="#dashboard" aria-label="Permalink to &quot;Dashboard&quot;">​</a></h3><ul><li><strong>Rotating feature tips</strong> — the empty state before repo selection cycles through ~20 localised tips every 30 s to surface features you might not have discovered</li></ul><h2 id="auto-update-version-v1-4" tabindex="-1">Auto-update &amp; version (v1.4) <a class="header-anchor" href="#auto-update-version-v1-4" aria-label="Permalink to &quot;Auto-update &amp; version (v1.4)&quot;">​</a></h2><p>On launch, GitWand checks the GitHub Releases feed for a newer version and shows a toast with a changelog link when one is available. The current version is displayed in the footer / About view.</p><h2 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h2><ul><li><strong>Language</strong>: French / English</li><li><strong>Theme</strong>: Light / Dark</li><li><strong>Commit signature</strong>: Optional GPG signing</li><li><strong>Diff mode</strong>: Side-by-side or inline (default)</li><li><strong>AI provider</strong>: Claude Code CLI / Claude API / OpenAI-compatible / Ollama (used for every AI feature above)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/desktop.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const desktop = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  desktop as default
};
