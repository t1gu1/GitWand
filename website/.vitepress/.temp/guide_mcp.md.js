import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"MCP Server","description":"","frontmatter":{},"headers":[],"relativePath":"guide/mcp.md","filePath":"guide/mcp.md"}');
const _sfc_main = { name: "guide/mcp.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="mcp-server" tabindex="-1">MCP Server <a class="header-anchor" href="#mcp-server" aria-label="Permalink to &quot;MCP Server&quot;">​</a></h1><p>GitWand ships an MCP (Model Context Protocol) server that exposes its conflict resolution engine to AI agents. Any MCP-compatible client can use it — Claude Code, Claude Desktop, Cursor, Windsurf, Continue, and others.</p><p>The server runs over <strong>stdio</strong> and requires no API keys or network access. It works entirely with local Git repositories.</p><h2 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h2><p>Use directly via npx (no install needed):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npx</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -y</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @gitwand/mcp</span></span></code></pre></div><p>Or install globally:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -g</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @gitwand/mcp</span></span></code></pre></div><p>Or register with Claude Code in a single command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">claude</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> mcp</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> gitwand</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> npx</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -y</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @gitwand/mcp</span></span></code></pre></div><p><code>@gitwand/mcp</code> is listed in the <a href="https://registry.modelcontextprotocol.io" target="_blank" rel="noreferrer">MCP Registry</a> and ships with npm <a href="https://docs.npmjs.com/generating-provenance-statements" target="_blank" rel="noreferrer">provenance attestations</a> — you can verify that every release was built from the public <code>devlint/GitWand</code> repo in CI.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><h3 id="claude-desktop" tabindex="-1">Claude Desktop <a class="header-anchor" href="#claude-desktop" aria-label="Permalink to &quot;Claude Desktop&quot;">​</a></h3><p>Add to <code>~/Library/Application Support/Claude/claude_desktop_config.json</code> (macOS) or the equivalent on your OS:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;mcpServers&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;gitwand&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;command&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;npx&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;args&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;@gitwand/mcp&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;--cwd&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;/path/to/your/repo&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><h3 id="claude-code" tabindex="-1">Claude Code <a class="header-anchor" href="#claude-code" aria-label="Permalink to &quot;Claude Code&quot;">​</a></h3><p>Add to <code>.claude/settings.json</code> in your project:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;mcpServers&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;gitwand&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;command&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;npx&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;args&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;@gitwand/mcp&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Claude Code also ships with dedicated slash commands — see <a href="#slash-commands">Slash Commands</a> below.</p><h3 id="cursor-windsurf-other-mcp-clients" tabindex="-1">Cursor / Windsurf / Other MCP Clients <a class="header-anchor" href="#cursor-windsurf-other-mcp-clients" aria-label="Permalink to &quot;Cursor / Windsurf / Other MCP Clients&quot;">​</a></h3><p>Use the same <code>command</code> + <code>args</code> pattern. Refer to your client&#39;s MCP documentation for the configuration file location.</p><h2 id="tools" tabindex="-1">Tools <a class="header-anchor" href="#tools" aria-label="Permalink to &quot;Tools&quot;">​</a></h2><p>The MCP server exposes 5 tools:</p><h3 id="gitwand-status" tabindex="-1"><code>gitwand_status</code> <a class="header-anchor" href="#gitwand-status" aria-label="Permalink to &quot;\`gitwand_status\`&quot;">​</a></h3><p>List conflicted files in the current repo with their complexity and auto-resolvability.</p><p><strong>Parameters:</strong></p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>cwd</code></td><td>string</td><td>No</td><td>Working directory. Defaults to server cwd.</td></tr></tbody></table><p><strong>Returns:</strong> JSON with file count, total conflicts, auto-resolvable count, and per-file breakdown by conflict type.</p><h3 id="gitwand-resolve-conflicts" tabindex="-1"><code>gitwand_resolve_conflicts</code> <a class="header-anchor" href="#gitwand-resolve-conflicts" aria-label="Permalink to &quot;\`gitwand_resolve_conflicts\`&quot;">​</a></h3><p>Auto-resolve trivial merge conflicts using GitWand&#39;s pattern-based engine. Writes resolved files to disk unless <code>dry_run</code> is true.</p><p><strong>Parameters:</strong></p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>cwd</code></td><td>string</td><td>No</td><td>Working directory</td></tr><tr><td><code>files</code></td><td>string[]</td><td>No</td><td>Specific files to resolve. Omit to auto-discover.</td></tr><tr><td><code>dry_run</code></td><td>boolean</td><td>No</td><td>Analyze without writing. Default: <code>false</code>.</td></tr><tr><td><code>policy</code></td><td>string</td><td>No</td><td>Merge policy: <code>prefer-ours</code>, <code>prefer-theirs</code>, <code>prefer-merge</code>, <code>prefer-safety</code>, <code>strict</code></td></tr></tbody></table><p><strong>Returns:</strong> Summary + per-file results with <code>resolutions</code> (what was auto-resolved) and <code>pendingHunks</code> (what needs human/LLM attention). Each resolution includes confidence scores and a full DecisionTrace.</p><h3 id="gitwand-preview-merge" tabindex="-1"><code>gitwand_preview_merge</code> <a class="header-anchor" href="#gitwand-preview-merge" aria-label="Permalink to &quot;\`gitwand_preview_merge\`&quot;">​</a></h3><p>Dry-run resolution on all conflicted files. Shows stats and a risk assessment (<code>low</code> / <code>medium</code> / <code>high</code>) without modifying any files.</p><p><strong>Parameters:</strong></p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>cwd</code></td><td>string</td><td>No</td><td>Working directory</td></tr></tbody></table><p><strong>Returns:</strong> Risk level, summary (total/resolvable/remaining), auto-resolve percentage, and full per-file detail.</p><h3 id="gitwand-explain-hunk" tabindex="-1"><code>gitwand_explain_hunk</code> <a class="header-anchor" href="#gitwand-explain-hunk" aria-label="Permalink to &quot;\`gitwand_explain_hunk\`&quot;">​</a></h3><p>Explain why a specific conflict hunk was classified as its type. Returns the full DecisionTrace with evaluation steps and the ours/theirs/base content.</p><p><strong>Parameters:</strong></p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>cwd</code></td><td>string</td><td>No</td><td>Working directory</td></tr><tr><td><code>file</code></td><td>string</td><td><strong>Yes</strong></td><td>Path to the conflicted file (relative to cwd)</td></tr><tr><td><code>line</code></td><td>number</td><td><strong>Yes</strong></td><td>Start line of the hunk to explain</td></tr></tbody></table><p><strong>Returns:</strong> Classification type, explanation, confidence (score + dimensions + boosters/penalties), trace steps, and the raw ours/theirs/base content.</p><h3 id="gitwand-apply-resolution" tabindex="-1"><code>gitwand_apply_resolution</code> <a class="header-anchor" href="#gitwand-apply-resolution" aria-label="Permalink to &quot;\`gitwand_apply_resolution\`&quot;">​</a></h3><p>Apply a custom resolution to a specific conflict hunk. Replaces the conflict markers at the given line with the provided content, then re-checks for remaining conflicts.</p><p><strong>Parameters:</strong></p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>cwd</code></td><td>string</td><td>No</td><td>Working directory</td></tr><tr><td><code>file</code></td><td>string</td><td><strong>Yes</strong></td><td>Path to the conflicted file</td></tr><tr><td><code>line</code></td><td>number</td><td><strong>Yes</strong></td><td>Start line of the conflict (the <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> line)</td></tr><tr><td><code>content</code></td><td>string</td><td><strong>Yes</strong></td><td>Resolved content to replace the conflict block</td></tr></tbody></table><p><strong>Returns:</strong> Confirmation + remaining conflict count in the file.</p><h2 id="resources" tabindex="-1">Resources <a class="header-anchor" href="#resources" aria-label="Permalink to &quot;Resources&quot;">​</a></h2><p>The server exposes 3 resources that clients can read for context:</p><table tabindex="0"><thead><tr><th>URI</th><th>Description</th></tr></thead><tbody><tr><td><code>gitwand://repo/conflicts</code></td><td>Current conflict state — file list, counts, types, auto-resolvability</td></tr><tr><td><code>gitwand://repo/policy</code></td><td>Active <code>.gitwandrc</code> configuration (or defaults if none exists)</td></tr><tr><td><code>gitwand://hunk/{file}/{line}</code></td><td>Raw hunk content for a specific conflict — ours/theirs/base + trace</td></tr></tbody></table><h2 id="the-collaboration-loop" tabindex="-1">The collaboration loop <a class="header-anchor" href="#the-collaboration-loop" aria-label="Permalink to &quot;The collaboration loop&quot;">​</a></h2><p>The MCP server enables a workflow where GitWand handles the trivial conflicts and the LLM tackles the complex ones:</p><ol><li><p><strong>Preview</strong> — The agent calls <code>gitwand_preview_merge</code> to understand the conflict landscape: how many files, how many conflicts, what percentage GitWand can handle, and the overall risk level.</p></li><li><p><strong>Auto-resolve</strong> — The agent calls <code>gitwand_resolve_conflicts</code>. GitWand resolves the trivial patterns (same_change, one_side_change, whitespace_only, etc.) and returns <code>pendingHunks</code> for everything it couldn&#39;t handle.</p></li><li><p><strong>Understand</strong> — For each pending hunk, the agent has the ours/theirs/base content, the classification trace explaining <em>why</em> GitWand flagged it as complex, and the confidence dimensions showing where the uncertainty lies.</p></li><li><p><strong>Resolve</strong> — The agent writes its own resolution and calls <code>gitwand_apply_resolution</code> to apply it. GitWand re-checks the file and reports remaining conflicts.</p></li><li><p><strong>Verify</strong> — The agent can call <code>gitwand_status</code> to confirm all conflicts are resolved, or read <code>gitwand://repo/conflicts</code> for a final check.</p></li></ol><p>This loop means the LLM never starts from scratch — it gets pre-triaged conflicts with full context, and only needs to handle the genuinely ambiguous cases.</p><h2 id="slash-commands" tabindex="-1">Slash Commands <a class="header-anchor" href="#slash-commands" aria-label="Permalink to &quot;Slash Commands&quot;">​</a></h2><p>GitWand ships <code>.claude/commands/</code> for Claude Code users:</p><h3 id="resolve" tabindex="-1"><code>/resolve</code> <a class="header-anchor" href="#resolve" aria-label="Permalink to &quot;\`/resolve\`&quot;">​</a></h3><p>Full conflict resolution workflow:</p><ol><li>Checks conflict status with <code>gitwand status</code></li><li>Auto-resolves trivial conflicts with <code>gitwand resolve --ci</code></li><li>Interprets the JSON output</li><li>Handles remaining complex conflicts with LLM reasoning</li><li>Verifies the result</li></ol><h3 id="preview" tabindex="-1"><code>/preview</code> <a class="header-anchor" href="#preview" aria-label="Permalink to &quot;\`/preview\`&quot;">​</a></h3><p>Merge preview and risk assessment:</p><ol><li>Simulates a merge with <code>git merge --no-commit --no-ff</code></li><li>Analyzes conflicts with <code>gitwand resolve --ci --dry-run</code></li><li>Assesses risk level</li><li>Recommends whether to proceed or abort</li><li>Cleans up the simulated merge</li></ol><h2 id="cli-flag" tabindex="-1">CLI flag <a class="header-anchor" href="#cli-flag" aria-label="Permalink to &quot;CLI flag&quot;">​</a></h2><p>The <code>--cwd</code> flag lets you point the server at a specific repository:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @gitwand/mcp</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --cwd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /path/to/repo</span></span></code></pre></div><p>If omitted, the server uses the current working directory.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/mcp.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const mcp = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  mcp as default
};
